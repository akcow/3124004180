# 开发期依赖
#
# 注意：本项目的**运行期依赖为零**，只用 Python 标准库。
# 上面这条约束是硬性的——评测机不保证装有任何第三方包，
# 所以 main.py / io_utils.py / preprocess.py / similarity.py 不得 import 以下任何东西。
#
# 下面的包只在开发、测试、静态检查、性能剖析时使用。

# ---- 单元测试与覆盖率 ----
pytest>=8.0
pytest-cov>=5.0

# ---- 静态检查（要求零警告）----
pylint>=3.0
flake8>=7.0

# ---- 性能剖析出图（tools/profiling.py 使用）----
matplotlib>=3.8
gprof2dot>=2024.1
