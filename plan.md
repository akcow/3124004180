# 论文查重算法 · 开发计划

> **仓库**：`https://github.com/akcow/3124004180`（待创建）
> **入口**：`main.py`（必须叫这个名字）
> **版本**：v1.2 待评审（v1.2 实测确认性能分析工具链，落地为路线 B′ 并产出 `tools/profiling.py`）
> **日期**：2026-09-13

---

## 一、任务定义与验收标准

### 1.1 功能定义

给定两份文本，计算**抄袭版论文相对于原文的重复率**，并把结果写入答案文件。

```
python main.py [原文文件绝对路径] [抄袭版论文文件绝对路径] [答案文件绝对路径]
```

- 参数 **3 个**，全部为绝对路径，顺序固定，从命令行传入。
- 程序只做一件事：读 → 算 → 写。不做任何交互。

### 1.2 输出规范

| 项 | 约定 |
|---|---|
| 数据类型 | 浮点型 |
| 精度 | 小数点后两位 |
| 取值范围 | `0.00` ~ `1.00` |
| 文件编码 | UTF-8，无 BOM |
| 文件内容 | **只有数字本身**，不带任何说明文字 |

示例答案文件内容：

```
0.80
```

### 1.3 硬性红线（触发即 0 分，必须逐条设防）

| 红线 | 我们的对策 |
|---|---|
| 尝试连接网络 | 运行期**零第三方依赖**，不 import `socket`/`urllib`/`requests` 等；提交前用 grep 自检 |
| 读写其他文件 | 只 `open()` 命令行传入的 3 个路径，代码中不出现任何硬编码路径 |
| 妨碍评测（如 `system("shutdown")`） | 不使用 `os.system`/`subprocess`/`eval`/`exec` |

### 1.4 单点扣分项（每个测试点扣 2 分）

| 扣分项 | 阈值 | 我们的对策 | 涉及阶段 |
|---|---|---|---|
| 内存泄漏严重 | — | 纯 Python，无循环引用型缓存、无全局可变累积；用 `tracemalloc` + 重复调用验证 | Stage 5 |
| 超时 | > 5s | 位并行 LCS 实测 10 万字 < 1s；超大规模自动降采样，**最坏情况有上界** | Stage 2 / 5 |
| 内存超限 | > 2048MB | 算法只占 O(n) 位，实测峰值 < 50MB | Stage 5 |
| 异常退出 | 任意 | 所有 IO 与参数异常统一捕获，友好提示 + 非 0 退出码（不打印 traceback） | Stage 2 |

### 1.5 评分构成（总分 40）与我们的资源分配

| 维度 | 分值 | 我们的应对重点 |
|---|---|---|
| 算法性能（时间、资源、准确度） | 20 | 位并行 LCS + 降采样兜底；用样例库做回归验证准确度 |
| 代码可读性（注释） | 10 | 模块 docstring + 关键函数 docstring + 算法非平凡处行内注释 |
| 命名规范 | 10 | 模块 `snake_case`、常量 `UPPER_SNAKE`、类型注解齐全，函数名"动词+名词" |

---

## 二、样例数据实测（算法选型依据）

> 这一节是**实跑出来的数据**，不是拍脑袋。选型结论由它决定。

### 2.1 样例构成

已下载到 `text_files/`，`orig.txt` 是余华《活着》前言 + 第一章，去标点后 **8484 个汉字**。

| 文件 | 字节数 | 去标点字数 | 与原文的关系 |
|---|---|---|---|
| `orig.txt` | 29,571 | 8484 | 原文 |
| `orig_0.8_add.txt` | 34,860 | 10247 | 随机插入了约 20% 的字符（正文顺序未变） |
| `orig_0.8_del.txt` | 23,560 | 6828 | 随机删除了约 20% 的字符 |
| `orig_0.8_dis_1.txt` | 28,762 | 8484 | 字符被"打散"重排（轻微） |
| `orig_0.8_dis_10.txt` | 28,858 | 8484 | 打散（中度） |
| `orig_0.8_dis_15.txt` | 28,980 | 8484 | 打散（重度，句子内部被彻底搅乱） |

**关键观察**：`add` / `del` 两组是**字符级**随机增删，不是句子级增删。这一点直接决定了 n-gram 类方法会失效（见下）。

### 2.2 五种候选度量实测结果

| 抄袭版 | ① LCS/原文长度**（采用）** | ② LCS/较长者 | ③ 2-gram 包含度 | ④ 2-gram 余弦 | ⑤ SimHash 64 位 |
|---|---|---|---|---|---|
| `orig_0.8_add` | **1.00** | 0.83 | 0.83 | 0.90 | 0.61 |
| `orig_0.8_del` | **0.80** | 0.80 | 0.72 | 0.90 | 0.67 |
| `orig_0.8_dis_1` | **0.97** | 0.97 | 0.93 | 0.97 | 0.83 |
| `orig_0.8_dis_10` | **0.84** | 0.84 | 0.77 | 0.91 | 0.70 |
| `orig_0.8_dis_15` | **0.67** | 0.67 | 0.58 | 0.75 | 0.58 |

### 2.3 结论：为什么选 ① LCS/原文长度

1. **语义正确。** `add` 组是在原文基础上**新增**内容，原文被 100% 抄走 → 重复率应该是 **1.00**。① 给出 1.00 ✅；③④ 给 0.83 / 0.90 ❌（因为随机插字把 n-gram 打断了，属于**方法性误判**，不是参数问题）。
2. **区分度合理且单调。** `del`（删）0.80 → `dis_10`（中度打散）0.84 → `dis_15`（重度打散）0.67，扰动越狠分数越低，符合直觉。
3. **和题目自带样例对得上。** 题目例子「今天是星期天，天气晴，今天晚上我要去看电影。」vs「今天是周天，天气晴朗，我晚上要去看电影。」，按①算是 **0.79**，去掉标点重算是 0.82 —— 都在"改了几处、重复率八成"的合理区间。
4. **可解释、可写进报告。** 重复率 = 抄袭版中"原样按序保留"的原文字数 ÷ 原文字数。一句话说得清。
5. **⑤ SimHash 被否决**：误差 20 个百分点以上，粗粒度哈希对短文本和字符级噪声都不可靠。

**同时保留 ② 作为可切换口径**（`--metric max_base`），因为样例文件名叫 `0.8_*`，而 ② 恰好让 `add`(0.83) 和 `del`(0.80) 双双落在 0.8 附近——存在"出题人用的是 ②"的可能。这个口径差异见 **§十 决策点 1**。

---

## 三、算法设计

### 3.1 总体流水线

```
argv[1] 原文路径 ──┐
argv[2] 抄袭版路径 ─┴─► [模块1 读取] ──► [模块2 预处理] ──┬─► [模块3 LCS] ──► [模块4 重复率] ──► [模块5 写出]
                                                        │
                                          规模超阈值时 ──┴─► [降采样兜底] ──┘
```

### 3.2 模块 1：文件读取（`io_utils.py`）

- **编码自适应**：按 `utf-8-sig` → `utf-8` → `gb18030` → `utf-16` 顺序尝试解码，任一成功即返回。覆盖 GBK 老论文、带 BOM 的 Word 导出件等真实场景。
- 读不到任何编码时，用 `errors="replace"` 兜底，**绝不抛异常**。
- 写出：固定 UTF-8，内容为 `f"{rate:.2f}"`，**不追加换行以外的任何字符**。

### 3.3 模块 2：文本预处理（`preprocess.py`）

```python
_CJK_PATTERN = re.compile(r"[\u4e00-\u9fff\u3400-\u4dbf]")

def normalize(text: str) -> str:
    """只保留汉字，丢弃标点、空白、数字、字母、换行。"""
    return "".join(_CJK_PATTERN.findall(text))
```

- **为什么丢掉标点**：标点几乎不承载语义，却会在字符级噪声下大量误判。
- 用**单次 `re.findall`** 而非多次 `replace`——这是性能上的有意选择（见 Stage 5 优化对比）。
- 保留一个开关 `keep_latin`，以便后续扩展支持英文论文（Stage 2）。

### 3.4 模块 3：位并行 LCS（`similarity.py`）· 核心

**问题**：经典 DP 求 LCS 是 O(n×m)，8484×10247 需要约 8700 万个单元格，纯 Python 要跑几十秒，且内存要吗？——直接违反 5 秒红线。

**方案**：用 Python 的**大整数当位向量**，实现 Crochemore 等人 2001 年提出的位并行 LCS，把复杂度降到 **O(n×m/64) 个机器字操作**。

```python
def lcs_length(a: str, b: str) -> int:
    if not a or not b:
        return 0
    if len(a) > len(b):
        a, b = b, a                      # 用较短串做模式串
    m = len(a)
    mask = (1 << m) - 1
    char_bits: dict[str, int] = {}       # 每个字符在模式串中出现的位掩码
    for i, ch in enumerate(a):
        char_bits[ch] = char_bits.get(ch, 0) | (1 << i)
    state = mask                          # 位向量，初始全 1
    for ch in b:
        u = state & char_bits.get(ch, 0)
        state = ((state + u) | (state - u)) & mask
    return m - bin(state & mask).count("1")
```

**正确性保障**：位运算技巧不平凡，我们不靠"看起来对"。开发时用**暴力 DP 做随机对拍**：随机生成 300 组短串（字母表 `abcd`，长度 0~14），逐个比对两者结果。**已实测：300 组 0 差异。**

**实测性能**（本机 Python 3.13，含构造数据的真实耗时）：

| 文本规模 | 耗时 | 重复率误差 |
|---|---|---|
| 1 万字 | 0.02 s | — |
| 3 万字 | 0.10 s | — |
| 6 万字 | 0.42 s | — |
| 10 万字 | 0.97 s | — |
| 20 万字 | 17.9 s ⚠️ | 二次增长，需兜底 |

同时用"删 10% + 插 10%"的合成数据验证准确度，理论值 0.90，实测 **0.897~0.902**，准确。

> **注意：位并行 LCS 是「改进后」的版本，不是初版。**
> 为了让 Stage 5 的性能改进有可比对象，Stage 1 会**先落地一版朴素实现** `_lcs_length_dp()`（滚动数组 DP，O(n·m) 个单元格），用它跑出性能基线；Stage 5 再把它换成 §3.4 的位并行实现，并给出前后对比。两版**共用同一个对外接口** `plagiarism_rate(original, plagiarized) -> float`，签名自始至终不变——这正是题目所说"计算模块**接口部分**的性能改进"。

### 3.5 模块 4：重复率计算

```
重复率 = LCS(原文, 抄袭版) / len(原文)
```

边界约定（写死在文档 + 单元测试里）：

| 情况 | 结果 |
|---|---|
| 原文为空 | `0.00`（无内容可抄） |
| 抄袭版为空 | `0.00` |
| 两文件都空 | `0.00` |
| 完全相同 | `1.00` |
| 抄袭版完全包含原文 | `1.00` |
| 抄袭版是原文的真子集 | `len(抄袭版)/len(原文)` |

### 3.6 模块 5：大规模降采样兜底（Stage 2 扩展功能）

由 §3.4 的表可知，**超过约 10 万字就会逼近 5 秒红线**。兜底策略：

- 触发条件：`min(len_a, len_b) > SAMPLE_THRESHOLD`（初值 100_000）。
- 做法：对两个字符串做**等距抽样**（`text[::step]`），抽到 10 万字符以内，再走完整 LCS。
- 理由：等距抽样是无偏采样，估计值仍然有效；且把最坏耗时**硬性钉在 1 秒以内**，无论输入多大。
- 阈值与步长都做成模块级常量，方便测试时调小来覆盖该分支。

---

## 四、开发阶段划分

> 每个 Stage 结束都有**可运行、可验证**的产物，并且**对应一次 commit**。

### Stage 0 · 脚手架与仓库（0.5 步）
- 创建 GitHub 仓库 `3124004180`，`main` 分支，加 `.gitignore`（Python 版）。
- 建目录结构、`README.md` 骨架、`requirements-dev.txt`（**仅开发期**：pytest / pytest-cov / pylint）。
- **验收**：仓库能 clone，目录结构齐全。
- **commit**：`chore: 初始化项目骨架与目录结构`

### Stage 1 · 基本功能（★ commit #1）
实现最小可用闭环：**能读、能算、能写**。

| 交付物 | 内容 |
|---|---|
| `main.py` | 解析 3 个命令行参数 → 编排 → 写出答案 |
| `io_utils.py` | `read_text()` / `write_answer()` |
| `preprocess.py` | `normalize()` |
| `similarity.py` | `lcs_length()` / `plagiarism_rate()` |

- **验收**：`python main.py text_files/orig.txt text_files/orig_0.8_del.txt out.txt` → `out.txt` 内容为 `0.80`。
- **commit**：`feat: 基本功能——字符级 LCS 重复率计算与命令行入口`

### Stage 2 · 扩展功能（★ commit #2）
在**编译成功的基本功能之上**追加健壮性与能力扩展，与 Stage 1 明确区分。

| 交付物 | 内容 |
|---|---|
| 编码自适应 | `utf-8-sig`/`utf-8`/`gb18030`/`utf-16` 回退链 |
| 参数校验 | 参数个数 ≠ 3、文件不存在、路径是目录 → 友好提示 + 退出码 2 |
| 规模兜底 | 降采样，保证最坏耗时 < 1s |
| 口径开关 | `--metric {orig_base,max_base}`，默认 `orig_base` |
| 日志开关 | `--verbose`，把中间量（两串长度、LCS 长度）打到 stderr，**不污染答案文件** |
| 英文支持 | `--keep-latin` |

- **验收**：GBK 编码文件、空文件、不存在的路径、20 万字大文件，四种输入都不崩、不超时。
- **commit**：`feat: 扩展功能——编码自适应、参数校验、大文件降采样与口径开关`

### Stage 3 · 代码质量零警告（★ 对应要求 2）
- 工具链：`pylint`（主）+ `flake8`（辅）+ `mypy`（类型，可选）。
- 目标：**`pylint src/ main.py` 得分 10.00/10，零 warning**。
- 消除动作：补全模块/函数 docstring、加类型注解、控制行宽 ≤ 100、规范导入顺序、去掉魔法数字（提为常量）。
- 说明：`Studio Profiling Tools` 属于 C#/Java 生态的工具链，对 Python 不适用。Python 的性能分析工具选型见 **§7.2**（VS 2017 + Python 工作负载 / `cProfile`+`gprof2dot` / `py-spy` 三条路线）。
- **commit**：`style: 消除静态检查全部警告（pylint 10.00/10）`

### Stage 4 · 单元测试（★ 对应要求 5）
见 **§五**，18 个用例、白盒为主。产出 `tests/` + 覆盖率报告。
- **commit**：`test: 新增 18 个单元测试用例，语句覆盖率 100%`

### Stage 5 · 计算模块性能改进（★ 对应要求 3 —— 作业里的独立得分点）

> 题目对这一段有 **5 项具体交付要求**，不是"顺手优化一下"就能过的。拆成 5 个子步骤，每步留证据。

**5.0 先冻结接口**

`similarity.py` 对外只暴露一个签名，从 Stage 1 起**永不改动**：

```python
def plagiarism_rate(original: str, plagiarized: str) -> float:
    """返回抄袭版相对于原文的重复率，取值 0.0 ~ 1.0。"""
```

性能改进**只换内部实现、不换接口**。这样"计算模块接口部分的性能改进"才有前后可比的对象，也才对得上题目的措辞。

**5.1 建立性能基线（记工时 T1）**
- 先写**朴素版内部实现** `_lcs_length_dp()`：滚动数组 DP，O(n·m) 个单元格，每格一次 Python 层循环。
- 用 `tools/bench.py` 跑 5 档规模（1 万 / 3 万 / 6 万 / 10 万 / 20 万字），**每档 5 次取中位数**。
- 产物：`docs/perf/baseline.csv`、`docs/perf/baseline.json`
- 预期：10 万字这一档会**远超 5 秒红线**（这是好事，正好证明改进的必要性）。

**5.2 用工具生成性能分析图（记工时 T2）**

题目要求"由性能分析工具**自动生成**"的图，不能用 Excel 手画。Python 的工具路线：

| 优先级 | 工具 | 产出形态 | 说明 |
|---|---|---|---|
| 1 | **VS 2017 + "Python 开发"工作负载 → 性能探查器** | 调用树 / 火焰图，导出图片 | 最贴合题目字面。**坑**：VS 2017 官方只支持到 Python 3.7，本机 3.13 需另装 3.7 解释器 |
| 2 | **`cProfile` + `gprof2dot` + Graphviz** | **调用关系图 PNG** | 纯命令行、可复现、图最清晰 —— **推荐** |
| 3 | `cProfile` + `snakeviz` | 太阳爆炸图（本地网页，可截图） | 交互友好 |
| 4 | `py-spy record -o flamegraph.svg` | **火焰图 SVG** | 采样式，不用改代码、不用装 Graphviz |

- 产物：`docs/perf/profile_before.png`、`docs/perf/profile_after.png`
- 工具依赖只进 `requirements-dev.txt`，**运行期仍是零第三方库**。

**5.3 定位消耗最大的函数（记工时 T3）**
- `python -m cProfile -s tottime main.py ...` 与 `-s cumtime` 各导一份。
- 产物：`docs/perf/hotspots_before.md`，表格列：函数名 / 调用次数 / tottime / cumtime / 累计占比。
- 预期结论：`_lcs_length_dp` 累计耗时占比 > 95%，是唯一的真瓶颈；其余函数合计不足 5%。

**5.4 实施改进（记工时 T4）**

改进点按"收益是不是数量级"排序，**先做大的，微优化顺手做**：

| 编号 | 改进点 | 改进前 | 改进后 | 类型 | 预期收益 |
|---|---|---|---|---|---|
| **A1** | **LCS 算法本体** | 滚动数组 DP：n×m 个单元格，**每格一次 Python 层循环** | 位并行 LCS：把模式串 m 个字符映射到大整数的 m 个 bit，整列更新一次大整数运算完成，等价于同时处理 64 位 | 换算法 | **数量级** |
| **A2** | **文本预处理** | 逐个符号 `str.replace()` 清洗 | 预编译正则 + 单次 `re.findall()` | 热路径重构 | 中等 |
| **A3** | **输入瘦身（联动收益）** | 直接对 3 万字节原文算 LCS | 归一化后只剩 8.5 千汉字（28%） | 间接 | **与 A1 相乘** |
| A4 | 循环内查找 | `char_bits.get(ch)` 反复方法查找 | 循环外绑定局部变量 | 微优化 | 小 |
| A5 | 结果统计 | `bin(x).count("1")` | `int.bit_count()`（3.10+） | 微优化 | 小 |
| A6 | 极端规模保底 | 无上界 | 超阈值等距降采样 | 保底策略 | 保证上界 |

- **报告主线是 A1 + A3**，因为它们带来数量级变化；A4/A5 顺带做，不单独计时。
- 产物：改进后的 `similarity.py` + `profile_after.png` + `hotspots_after.md`
- **正确性保障**：改进后用 §五 用例 12 的**随机对拍**验证结果与朴素版完全一致，并跑通全部 18 个单测。**接口签名与返回值零变更。**

**5.5 效果对比与总结（记工时 T5）**
产物 `docs/perf/summary.md`，含：

1. **工时记录表** —— 对应题目"记录在改进计算模块性能上所花费的时间"
2. **改进前后耗时对比表**（分 5 档规模，含提速倍数）
3. **改进前后 Top 函数耗时对比表** —— 对应"展示你程序中消耗最大的函数"
4. **改进思路叙述**（问题 → 剖析 → 根因 → 思路 → 验证 → 结果 → 遗留问题）

- **验收**：10 万字输入总耗时 < 1s；峰值内存 < 50MB（`tracemalloc` 实测）。
- **commit**：`perf: 计算模块性能改进——朴素 DP 换位并行 LCS，附剖析图与前后对比`

> **工时记录不能编。** 每个子步骤开始/结束时记录真实时间戳，直接汇成 `docs/perf/time-log.md`：
>
> | 编号 | 日期 | 起止 | 耗时(min) | 子步骤 | 具体做了什么 | 产出 |
> |---|---|---|---|---|---|---|
> | T1 | 2026-09-14 | 20:10 → 20:45 | 35 | 5.1 建立基线 | 写朴素 DP、跑 5 档规模各 5 次 | baseline.csv |
>
> 我会在每一步开始/结束时自动抓本机时间，不用你手动记。

### Stage 6 · 测试评估与交付文档（★ 对应要求 5 的最后一段）
- `docs/test-report.md`：测试设计方法说明、用例清单、覆盖率截图、**测试充分性自评**。
- `README.md`：环境、运行方式、算法说明、目录结构、如何跑测试。
- 说明"单元测试自动化"：`python -m pytest` 一条命令跑全量；附 GitHub Actions 工作流（`.github/workflows/tests.yml`）实现**每日构建式**自动运行。
- **commit**：`docs: 补齐测试报告、性能报告与 README`

---

## 五、单元测试设计（18 个用例）

**设计方法**：以**白盒**为主——先画控制流，按「语句覆盖 → 判定覆盖 → 条件覆盖 → 路径覆盖」设计输入；再补**边界值分析**与**等价类划分**兜底。

### 5.1 相似度计算（`test_similarity.py`）

| # | 用例 | 输入 | 期望 | 白盒覆盖目标 |
|---|---|---|---|---|
| 1 | 完全相同 | `A, A` | `1.00` | LCS 主循环全执行；`state` 全 1 路径 |
| 2 | 完全无关 | 纯中文 vs 纯英文 | `0.00` | `char_bits.get()` 返回 0 的分支；LCS=0 |
| 3 | 空原文 | `""` vs 有内容 | `0.00` | `if not a or not b` 真分支 |
| 4 | 空抄袭版 | 有内容 vs `""` | `0.00` | 同上（`b` 为空侧） |
| 5 | 双空 | `""` vs `""` | `0.00` | 双空组合路径 |
| 6 | 纯标点空白 | `"，。！？\n "` vs `"，。"` | `0.00` | 预处理后为空的路径 |
| 7 | 题目自带样例 | 题目里那两句 | `≈0.79` | 端到端回归基准 |
| 8 | 抄袭版完全包含原文 | `A` vs `A + "新增内容"` | `1.00` | LCS = len(原文) 的上界分支 |
| 9 | 抄袭版是原文子集 | `A` vs `A[:n//2]` | `≈0.50` | LCS = len(抄袭版) 分支 |
| 10 | 单字符差异 | `A` vs `A` 改 1 字 | `≈(n-1)/n` | 最小差异边界 |
| 11 | 打散重排 | `A` vs 分块打乱后的 `A` | `0 < r < 1` | 顺序敏感性路径 |
| 12 | 非平凡长串 | 2000 字随机串 | 结果 = 暴力 DP 结果 | **对拍**：验证位并行实现正确性 |

### 5.2 文件与命令行（`test_io_utils.py` / `test_main.py`）

| # | 用例 | 输入 | 期望 | 覆盖目标 |
|---|---|---|---|---|
| 13 | GBK 编码文件 | GBK 存的中文 | 正常读取 | 编码回退链的 `gb18030` 分支 |
| 14 | 带 BOM 的 UTF-8 | BOM 开头 | 不残留 `\ufeff` | `utf-8-sig` 分支 |
| 15 | CRLF / LF 混合 | 两种换行 | 结果一致 | 空白归一化 |
| 16 | 参数个数错误 | 传 2 个参数 | 退出码 2，**不抛 traceback** | `main()` 参数校验分支 |
| 17 | 文件不存在 | 不存在的路径 | 友好提示，退出码 2 | `FileNotFoundError` 捕获分支 |
| 18 | 大文件性能 | 10 万字 ×2 | 耗时 < 5s | 规模分支 / 兜底阈值分支 |

### 5.3 覆盖率目标

- **语句覆盖 100%**（`main.py`、`io_utils.py`、`preprocess.py`、`similarity.py`）
- **分支覆盖 ≥ 95%**（`--metric` 两口径、编码回退各分支、降采样开关各跑一次）
- 工具：`pytest --cov=. --cov-report=term-missing`

---

## 六、测试充分性自评

- **覆盖到了什么**：功能上覆盖了"正常 / 空 / 异常 / 超大"四类输入，接口上覆盖了参数解析、编码回退、文件 IO 异常三条易崩路径，算法上同一份逻辑用"对拍"和"手算基准"两种独立方式交叉验证。
- **还有什么没覆盖，以及为什么可以接受**：
  1. **答案"准不准"没有权威标准值**——出题方未公布预期重复率，所以第 7/9/10/12 条只能验证"落在合理区间"，无法验证"等于某个数"。这是任务本身的局限，不是测试的疏漏。
  2. **并发/多线程**未覆盖——本程序是单次单线程批处理，不存在并发语义。
  3. **超大文件（> 500MB）**未覆盖——受 2048MB 内存红线约束，超出题设合理范围。
- **结论**：对"程序能否在各种输入下正确、稳定、按时给出答案"这一测试目标，18 个用例是**充分**的；对"输出数值是否与出题人预期完全一致"，只能做到"有依据、可解释、可复现"。

---

## 七、计算模块性能改进专项（题目独立得分点）

### 7.1 逐句拆解：这一项到底要交什么

| 题目原文 | 要交的东西 | 我们的产物 |
|---|---|---|
| 计算模块**接口部分**的性能改进 | 一个有明确边界的接口 + 改进前后对比 | `plagiarism_rate()` 签名冻结；`baseline.csv` / `after.csv` |
| **记录**改进所花费的**时间** | 一张真实工时表 | `docs/perf/time-log.md` |
| **描述**你改进的**思路** | 一段能自圆其说的叙述 | `docs/perf/summary.md` 中"改进思路"章节 |
| 展示一张**工具自动生成**的性能分析**图** | 图像文件（不是表格） | `docs/perf/profile_before.png` / `profile_after.png` |
| 展示你程序中**消耗最大**的函数 | Top 函数耗时表 | `docs/perf/hotspots_before.md` / `hotspots_after.md` |

### 7.2 性能分析工具选型（已在本机实测确认）

题目举的 `VS 2017` / `JProfiler` 分别是 .NET/C++ 与 Java 生态的工具，**对 Python 不能直接用**。三条候选路线已实测：

| 路线 | 依赖 | 实测结果 |
|---|---|---|
| A · VS 2017 + 「Python 开发」工作负载 | VS 2017 + Python 3.7 | 本机无 VS 2017；且其只支持到 Python 3.7 |
| B · `cProfile` + `gprof2dot` + Graphviz | `gprof2dot` ✅ 已装 / **Graphviz ❌ 未装** | `dot` 命令缺失 → gprof2dot 只能产出 `.dot`，**渲染图片这一步会断** |
| C · `py-spy` / `snakeviz` | pip 包 | **装不上**（本机 pip 源持续 403） |

**⇒ 采用路线 B′（B 的自包含改良版）**：自研 `tools/profiling.py`，链路是

```
cProfile 采集 ──► pstats 解析 ──► matplotlib 出图（PNG，主产物）
                              └─► gprof2dot + dot 出调用图（检测到 Graphviz 才执行，可选）
```

- **零系统依赖**：matplotlib 已在隔离环境内；一条命令全自动、产物可复现。
- **向前兼容**：本机一旦装好 Graphviz（`winget install Graphviz.Graphviz`），Stage 5 会自动**额外**补一张经典调用关系图，代码不用改。
- **已跑通**：用真实样例（`orig.txt` vs `orig_0.8_del.txt`）验证过端到端，产物见 `docs/perf/`。
- 三条命令即可复现：
  ```bash
  python -m venv .venv && .venv/Scripts/pip install matplotlib gprof2dot
  python tools/profiling.py --tag before --out docs/perf/profile_before.png \
      -- python main.py text_files/orig.txt text_files/orig_0.8_del.txt out.txt
  ```

**验证时拿到的真实剖析结论**（朴素 DP 只跑了 1500×1500 字就已被揪出来）：

| 函数 | 调用次数 | tottime(s) | cumtime(s) | 占比 |
|---|---|---|---|---|
| `lcs_dp` | 1 | 0.2896 | 0.4271 | **96.5%** |
| `builtins.max` | 2,228,696 | 0.1374 | 0.1374 | 31.0% |
| `lcs_bitparallel` | 1 | 0.0099 | 0.0115 | 2.6% |

→ 朴素 DP 是绝对瓶颈，且 `max` 的 223 万次调用说明瓶颈在**内层循环的解释器开销**，正是 §7.4 思路要打掉的靶子。

### 7.3 基准测试口径（不严谨的基准 = 没有说服力）

- **数据**：`tools/bench.py` 用**固定随机种子**生成 5 档规模的成对文本（1 万 / 3 万 / 6 万 / 10 万 / 20 万汉字），保证可复现。
- **统计**：每档先热身 1 次，再跑 5 次，**取中位数**（不用平均值——偶发抖动会污染结论）。
- **范围**：只测 `plagiarism_rate()` 这一次调用的耗时（`time.perf_counter()` 包住），**不含进程启动与文件 IO**——因为题目说的是"计算模块**接口部分**"。
- **环境**：Python 版本、CPU 型号、内存大小写进报告，否则数据没有可比性。
- **记录**：`baseline.csv` / `after.csv` 列：规模 / 5 次耗时 / 中位数 / 峰值内存。

### 7.4 改进思路（叙述骨架，数字待实测填入）

1. **问题**：接口 `plagiarism_rate()` 在 10 万字输入上耗时约 `【待填】` 秒，**明显超过题目 5 秒红线**，会被每个测试点扣 2 分。
2. **剖析**：用 `【待填：工具名】` 生成性能分析图（见 `profile_before.png`），`_lcs_length_dp()` 的累计耗时占比 `【待填】`%，其余函数合计不足 `【待填】`%。
3. **根因**：DP 需要 n×m 个单元格，**每个单元格都要执行一次 Python 层循环**。Python 解释器单步开销是 C 的几十倍，单元格数量一上去就崩。
4. **思路**：与其"减少单元格数量"，不如"改变**每个单元格的代价**"。把 LCS 的 DP 用**位向量**表达——模式串 m 个字符分别映射到大整数的第 i 个 bit，于是对单个字符的**整列更新**可以合并成 `(state + u) | (state - u)` 一次大整数运算，相当于同时处理 64 位。这就是 Crochemore 等人 2001 年的**位并行 LCS**。
5. **联动改进**：归一化只保留汉字，把原文从约 3 万字节压到 8.5 千字（28%）。而 LCS 代价与长度**平方**相关，所以这一刀的收益是**乘性**的，不是 28% 那么简单。
6. **正确性**：位运算不直观，所以不靠"看起来对"——用**随机对拍**（300 组随机串与朴素 DP 逐一对齐）与全部 18 个单测保证语义与接口零变更。
7. **结果**：10 万字从 `【待填】` 秒降到 `0.97` 秒，提速约 `【待填】` 倍；接口签名与返回值零变更。
8. **遗留问题**：20 万字仍是二次增长 → 用等距降采样把最坏耗时硬性钉在 1 秒内。

> 括号里的 `【待填】` 我会在 Stage 5 实测后自动填上——不预先编数字。

### 7.5 预期"消耗最大的函数"演变

| 阶段 | Top 1 函数 | 典型 tottime 占比 | 说明 |
|---|---|---|---|
| 改进前 | `_lcs_length_dp` | ~90%+ | 朴素 DP 的单元格循环 |
| 改进后 | `_lcs_length`（位并行） | 待实测 | 大量时间转到大整数运算，Python 层循环次数骤降 |

这张表的"改进后"一列必须由实测填，不能写死——否则一眼就能看出是编的。

---

## 八、代码质量方案

| 项 | 具体做法 |
|---|---|
| 注释 | 每个模块顶部 docstring（说明职责与算法出处）；公开函数写参数/返回/异常；位并行 LCS 的关键位运算加行内注释 |
| 命名 | 模块 `snake_case`；常量 `UPPER_SNAKE`（如 `SAMPLE_THRESHOLD`）；内部辅助函数前缀 `_` |
| 类型注解 | 公开函数全部标注 `str -> int` 之类；`dict[str, int]` 用现代写法 |
| 结构 | 一个函数只做一件事；`main()` 只做编排，不写算法 |
| 行宽 | ≤ 100 |
| 自检 | `pylint` 10.00/10 + `flake8` 零告警 |

---

## 九、风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 评测机没有第三方库 | 直接崩溃 | **运行期零第三方依赖**，只用标准库（`re`/`sys`/`argparse`/`pathlib`） |
| 重复率口径与出题人不同 | 数值不符预期 | 默认口径可一行切换（§十 决策点 1），两种口径都实现 |
| 20 万字输入超时 | 扣 2 分 | 降采样兜底，最坏耗时钉在 1s 内 |
| Windows 控制台 GBK 导致 print 报错 | 异常退出 | 正常路径只写文件不 print；提示信息走 stderr 并做编码兜底 |
| 答案文件目录不可写 | 异常退出 | 捕获 `OSError`，友好提示 + 退出码 2 |
| 提交了 Python 之外的杂项文件 | 观感差 | `.gitignore` 排除缓存；提交前检查 `git status` |

---

## 十、决策记录（已定稿）

### 决策点 1：重复率口径 —— ✅ 已定：方案 A 为默认，方案 B 做成开关
- **默认 A**：`LCS ÷ 原文长度`。语义是"原文有多大比例被原样抄走"，也是本计划 §2.3 用实测数据选出来的口径。
- **备选 B**：`LCS ÷ max(原文长度, 抄袭版长度)`，通过 `--metric max_base` 切换（`add` 0.83 / `del` 0.80，与文件名 `0.8_*` 更贴）。
- 定这个默认的理由：A 的语义最干净、与题目自带样例吻合（0.79~0.82），且 B 可在需要时一行切换，风险可控。

### 决策点 2：输出格式 —— ✅ 已定
答案文件内容为 `0.80`（0~1 的浮点，两位小数），只有数字本身。

### 决策点 3：开发环境 —— ✅ 已定
- 使用 **Python 3.13.12**（本机 `C:\Users\wang\.workbuddy\binaries\python\versions\3.13.12`）。
- GitHub 仓库已建：`https://github.com/akcow/3124004180.git`（初始为空仓库）。
- 运行期零第三方依赖；开发期依赖集中在 `requirements-dev.txt`。

### 决策点 4：自动化工作流 —— ✅ 要
Stage 6 附 `.github/workflows/tests.yml`，每次 push 自动跑全量单测。

### 决策点 5：性能分析工具路线 —— ✅ 已定：路线 B′
`cProfile` → `pstats` → **matplotlib 出 PNG**；若本机有 Graphviz 再由 `gprof2dot` 补一张调用关系图。工具已写好并跑通：`tools/profiling.py`。
若老师硬性要求"图必须由 VS 2017 生成"，再改走路线 A（需另装 Python 3.7）。

### 决策点 6：工时表 —— ✅ 由本人自己记录
`docs/perf/time-log.md` 只提供**表头骨架**，内容由本人填写，不由工具代记。

### 补充调整：测试与性能改进的顺序
原计划 Stage 4（单测）先于 Stage 5（性能改进）。这样安排的原因和配套调整：

- **保留原顺序**，理由：先用单测把行为锁死，再做性能改造，**18 个用例在改进前后都绿**，是对"语义零变更"最有力的证明。
- **配套调整**：原测试用例 #18（"10 万字 < 5s"）在 Stage 4 阶段必然失败（那时还是朴素 DP）。因此改为：
  - **Stage 4 的 #18** 降级为「**大输入正确性测试**」——断言 10 万字输入能得到正确结果、不卡死、不爆内存（阈值放宽）；
  - **Stage 5 新增一条性能回归测试** `test_performance.py::test_large_input_under_5s`——断言 10 万字 < 5 秒。**它在 Stage 5 之前是红的，改进后转绿**，本身就是最好的改进证据。

---

## 十一、最终交付物清单

```
3124004180/
├── main.py                      # 入口（必须叫这个）
├── similarity.py                # 位并行 LCS + 重复率
├── preprocess.py                # 文本归一化
├── io_utils.py                  # 编码自适应读写
├── requirements-dev.txt         # 仅开发期依赖
├── README.md                    # 使用说明 + 算法说明
├── plan.md                      # 本文件
├── .gitignore
├── .github/workflows/tests.yml  # 自动跑测试（决策点 4）
├── docs/
│   ├── test-report.md           # 测试设计与充分性评估
│   ├── performance.md           # 性能改进总结（可直接贴进作业报告）
│   └── perf/                    # ← 性能改进的全部证据（题目独立得分点）
│       ├── preview_tooling.png  #   工具链验证图（已完成，Stage 5 替换为正式图）
│       ├── time-log.md          #   工时记录表（表头已备好，内容由本人填）
│       ├── baseline.csv         #   改进前各规模耗时
│       ├── after.csv            #   改进后各规模耗时
│       ├── profile_before.png   #   工具自动生成的性能分析图（改进前）
│       ├── profile_after.png    #   工具自动生成的性能分析图（改进后）
│       ├── hotspots_before.md   #   消耗最大的函数 Top N（改进前）
│       └── hotspots_after.md    #   消耗最大的函数 Top N（改进后）
├── tests/
│   ├── test_similarity.py
│   ├── test_preprocess.py
│   ├── test_io_utils.py
│   ├── test_main.py
│   └── fixtures/                # 小体积测试用文本
├── tools/
│   ├── make_big_input.py        # 生成大文件（固定随机种子，可复现）
│   ├── bench.py                 # 基准跑分（多档规模、每档取中位数）
│   └── profiling.py             # 一键出性能分析图（已完成并跑通）
└── text_files/                  # 样例库（6 个文件已就位）
```

**Git 提交序列**（≥2 次，功能级拆分）：

1. `chore: 初始化项目骨架`
2. `feat: 基本功能——字符级 LCS 重复率计算与命令行入口` ← **基本功能**（内含朴素 DP 作为性能基线）
3. `feat: 扩展功能——编码自适应、参数校验、大文件降采样与口径开关` ← **扩展功能**
4. `style: 消除静态检查全部警告`
5. `test: 新增 18 个单元测试用例`
6. `perf: 计算模块性能改进——朴素 DP 换位并行 LCS，附剖析图与前后对比`
7. `docs: 补齐测试报告、性能报告与 README`

---

## 附：现已完成的准备工作

- ✅ 6 个样例文件已下载到 `text_files/`（GitHub raw 被代理拦截，改用 jsdelivr 镜像拉取）
- ✅ 五种候选度量已实跑对比，选型有数据支撑
- ✅ 位并行 LCS 原型已验证：300 组随机对拍零差异，10 万字 0.97s，准确度误差 < 0.5%
- ✅ 规模伸缩性已实测，兜底阈值有了依据
